{% extends template() %}

{% block title %}NingenShoppu | Modifier mon email {% endblock %}

{% block body %}
<div class="breadcrumb-container">
    <div class="breadcrumb flat">
        <a href="{{path('home')}}">
            <img src="{{asset('img/icons/home.svg')}}" alt="Accueil">
        </a>
        <a href="{{path('user_home')}}">Mon compte</a>
        <a href="#" class="active">Modifier mon email</a>
    </div>
</div>

<h1>Modifier mon email</h1>

{% form_theme form 'shared/forms/_form_theme.html.twig' %}
{{form_start(form)}}
  {{form_row(form.email)}}
  <button id="email-modify" class="btn secondary" type="submit">Envoyer</button>
{{form_end(form)}}
{% endblock %}